extends: electron-builder.yml
publish:
  provider: github
  owner: nick-neely
  repo: pilog
  channel: preview
  releaseType: prerelease
# Preview artifacts use the full prerelease package version, e.g.
# Pilog-0.1.0-preview.1.dmg. electron-updater reads preview.yml.
